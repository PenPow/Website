---
import { projects } from "../site.config";
import ProjectCard from "./internal/ProjectCard.astro";
---

<div class="grid w-full max-w-3xl grid-cols-1 gap-2 md:grid-cols-2">
	{projects.map(project => (<ProjectCard project={project} />))}
</div>